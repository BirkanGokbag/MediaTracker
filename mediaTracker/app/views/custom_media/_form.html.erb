<%= form_with(model: custom_medium, local: true) do |form| %>
  <% if custom_medium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_medium.errors.count, "error") %> prohibited this custom_medium from being saved:</h2>

      <ul>
      <% custom_medium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :custom_medium_type %>
  </div>

  <div class="field">
    <%= form.label :general_medias_id %>
    <%= form.text_field :general_medias_id, id: :custom_medium_general_medias_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
